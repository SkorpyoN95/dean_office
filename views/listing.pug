extends main

block content
    h3 All #{group} in system:
    ul(class='list-group') 
        each val in collection
            li(class='list-group-item')
                a(href=val.url) #{val.name}